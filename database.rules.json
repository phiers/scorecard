{
  "rules": {
    "public": {
      ".read": "true",
      ".write": "auth !== null"
    },
    "users": {
      "$userId": {
        ".read": "true",
        ".write": "$userId === auth.uid"
      }
    }
  }
}
