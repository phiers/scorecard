{
  "rules": {
    "public": {
      ".read": "auth !== null",
      ".write": "auth !== null"
    },
    "users": {
      "$userId": {
        ".read": "auth != null",
        ".write": "$userId === auth.uid"
      }
    }
  }
}
