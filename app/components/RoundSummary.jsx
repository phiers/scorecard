import React, { PropTypes } from 'react';
import { connect } from 'react-redux';

const RoundSummary = () => {
  return (
    <div>
      Round Summary Screen
    </div>
  );
};

export default connect()(RoundSummary);
