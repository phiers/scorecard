import React, { PropTypes } from 'react';
import TitleBar from 'Titlebar'; // eslint-disable-line

const RoundHole = () => {
  return (
    <div>
      <TitleBar title="Hole Scoring" />
    </div>
  );
};

export default RoundHole;
